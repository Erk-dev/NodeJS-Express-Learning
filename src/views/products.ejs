<!DOCTYPE html>
<html>
  <head></head>
  <body>
    <h1>Product อื่น ๆ</h1>
    <% products.map((product) => { %>
    <h4><a href="/products/<%=product.id%>"<%=product.productTitle %></h4>
    <p><%=product.productDescription%></p>
    <p>ราคา : <%=product.productPrice %> บาท</p>
    <%})%>
  </body>
</html>
